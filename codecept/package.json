{
  "name": "codeceptJS",
  "version": "1.0.0",
  "description": "",
  "main": "index.js",
  "scripts": {
    "test": "echo \"Error: no test specified\" && exit 1",
    "test:run": "npx codeceptjs def && npx codeceptjs run",
    "test:steps": "npx codeceptjs def && npx codeceptjs run --steps"
  },
  "keywords": [],
  "author": "",
  "license": "ISC",
  "devDependencies": {
    "codeceptjs": "^3.3.5",
    "playwright": "^1.26.0"
  },
  "dependencies": {
    "@codeceptjs/configure": "^0.10.0",
    "codeceptjs-assert": "^0.0.5"
  }
}
